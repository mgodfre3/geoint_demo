services:
  globe-server:
    build:
      context: .
      dockerfile: server/Dockerfile
    ports:
      - "3000:3000"
    environment:
      - ALERT_PROCESSOR_URL=http://alert-processor:8080
    restart: unless-stopped
